apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: my-app
spec:
  containers:
    - first-container:
      image: busybox
      command: ['sh', '-c', 'Hello from Container One! && sleep 300s']
